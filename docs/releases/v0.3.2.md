# v0.3.2 · 固定渠道模型清单统一

## 新增

- **GRSAI 新增 3 个 GPT 系模型**（按 image-studio 技能实测情报写死）：
  - `GPT Image 2`（¥0.03）：透明底专用，真 RGBA 直出（透明底开关生效）
  - `GPT Image 2.5 · Flare`（¥0.15）：图生图主力线，4K 直出约 32 秒，改色忠实
  - `GPT Image 2.5 · Sunburst`（¥0.15）：图生图精修线，细节更佳
  - GPT 系走网关 `/v1/api/generate` 通道，尺寸按官方像素表精确折算
- **aifast 渠道改为固定模型清单**：7 个实测可用模型直接可用（Gemini 3 Pro/Flash 的 token·preview·hy 线 + GPT Image 2），不再需要「获取模型→勾选」，填密钥即用；已下架模型不再出现

## 修复

- **GRSAI 健康检测误报**：GRSAI 网关本身不提供模型列表接口，此前探活显示「渠道可达，但密钥无效或无模型列表」造成误解；现显示绿色「网关可达（无模型列表接口，密钥生成时验证）」。密钥真实无效（401/403）时仍显示黄色警告
- **GRSAI 价格口径**：点数换算基准对齐官方（积分÷20000=元），nano-banana-2 显示 ¥0.06（此前误显 ¥0.13）、nano-banana-pro 显示 ¥0.09
